class Course {
  String courseId;
  String courseName;
  String status; // 'registered' or 'not-registered'

  Course({
    required this.courseId,
    required this.courseName,
    required this.status,
  });
}
